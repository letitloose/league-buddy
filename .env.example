# Reference copy only — do not put real credentials here.
# Copy the relevant lines into .env / .env.dev / .env.testdb and fill in
# real values (those files are gitignored).

DBHOST=db
DBPORT=3306
MYSQL_ROOT_PASSWORD=change_me
MYSQL_DATABASE=league_buddy
MYSQL_USER=leaguebuddy
MYSQL_PASSWORD=change_me

# true tears down and re-seeds the schema on boot, and (re)creates the
# default admin below. Never set this against a database with real data.
RESETDB=false

# Default admin account, created only when RESETDB=true.
LEAGUEBUDDYUSER=admin@example.com
LEAGUEBUDDYPASSWORD=change_me

# Optional bio for the default admin's player profile (first/last name
# required to seed one at all; the rest are optional). Useful in dev so your
# own login has a real profile instead of a blank placeholder one.
# LEAGUEBUDDYFIRSTNAME=
# LEAGUEBUDDYLASTNAME=
# LEAGUEBUDDYDOB=1990-01-01
# LEAGUEBUDDYPHONE=555-555-5555
# LEAGUEBUDDYADDRESS1=
# LEAGUEBUDDYCITY=
# LEAGUEBUDDYSTATE=
# LEAGUEBUDDYZIP=

# Optional test logins, one per role below system admin, created only when
# RESETDB=true and attached to the seeded league/team. Each is independently
# optional — leave a pair unset to skip creating that login.
# LEAGUEBUDDY_LEAGUEADMIN_EMAIL=league.admin@example.com
# LEAGUEBUDDY_LEAGUEADMIN_PASSWORD=change_me
# LEAGUEBUDDY_CAPTAIN_EMAIL=team.captain@example.com
# LEAGUEBUDDY_CAPTAIN_PASSWORD=change_me
# LEAGUEBUDDY_PLAYER_EMAIL=roster.player@example.com
# LEAGUEBUDDY_PLAYER_PASSWORD=change_me

# Mailjet credentials. Leave EMAIL_USER unset to skip email sending
# entirely (signup/reset/roster-invite emails are then just skipped, not
# fatal) — useful for local dev without a Mailjet account.
# EMAIL_USER=your_mailjet_api_key
# EMAIL_PASSWORD=your_mailjet_secret_key
# EMAIL_SENDER=noreply@yourdomain.com

# Twilio credentials, for text reminders/phone verification. Leave
# SMS_ACCOUNT_SID unset to skip SMS sending entirely (verification codes
# and text reminders are logged instead of sent, not fatal) — useful for
# local dev without a Twilio account. SMS_FROM_NUMBER must be an
# SMS-capable Twilio number, in E.164 format (e.g. +15551234567).
# SMS_ACCOUNT_SID=your_twilio_account_sid
# SMS_AUTH_TOKEN=your_twilio_auth_token
# SMS_FROM_NUMBER=+15551234567

# Bind address for the Go HTTP server.
SITE_HOST=localhost
SITE_PORT=8080

# Single canonical hostname the app itself uses to build links in emails
# (activation, password reset, invites, join-request notices). Kept separate
# from VIRTUAL_HOST below — nginx-proxy/Let's Encrypt accept a comma-
# separated list of hostnames there (e.g. "example.com,www.example.com"),
# which would otherwise end up baked verbatim into every generated link.
PUBLIC_HOST=localhost:8081

# Read by nginx-proxy (routing) and its Let's Encrypt companion (cert
# issuance) — not by the app itself. May be a comma-separated list.
VIRTUAL_HOST=localhost:8081

# Directory of .sql files applied on boot by the migration runner.
MIGRATION_PATH=./sql/migrations/

# --- Production-only (docker-compose.yml + nginx-compose.yml) ---
# PUBLIC_HOST=league-buddy.example.com
# VIRTUAL_HOST=league-buddy.example.com,www.league-buddy.example.com
# LETSENCRYPT_HOST=league-buddy.example.com,www.league-buddy.example.com
# LETSENCRYPT_EMAIL=you@example.com
# SOFTWARE_LAST_UPDATE=   (stamped automatically by the deploy job once it's enabled)
